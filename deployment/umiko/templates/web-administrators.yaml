apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.webAdministrators.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.webAdministrators.name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.webAdministrators.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.webAdministrators.name }}
    spec:
      automountServiceAccountToken: false
      containers:
        - name: {{ .Values.webAdministrators.name }}
          image: {{ .Values.webAdministrators.image }}:{{ .Values.appTag }}
          ports:
            - containerPort: {{ .Values.webAdministrators.containerPort }}
          resources:
            requests:
              cpu: {{ .Values.webAdministrators.resources.requests.cpu }}
              memory: {{ .Values.webAdministrators.resources.requests.memory }}
              ephemeral-storage: {{ index .Values.webAdministrators.resources.requests "ephemeral-storage" }}
            limits:
              cpu: {{ .Values.webAdministrators.resources.limits.cpu }}
              memory: {{ .Values.webAdministrators.resources.limits.memory }}
              ephemeral-storage: {{ index .Values.webAdministrators.resources.limits "ephemeral-storage" }}
          livenessProbe:
            httpGet:
              path: /health.txt
              port: {{ .Values.webAdministrators.containerPort }}
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health.txt
              port: {{ .Values.webAdministrators.containerPort }}
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.webAdministrators.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.webAdministrators.name }}
spec:
  selector:
    app: {{ .Values.webAdministrators.name }}
  ports:
    - port: {{ .Values.webAdministrators.servicePort }}
      targetPort: {{ .Values.webAdministrators.containerPort }}
      protocol: TCP