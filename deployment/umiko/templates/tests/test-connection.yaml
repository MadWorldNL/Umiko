apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "umiko.fullname" . }}-test-connection"
  labels:
    {{- include "umiko.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "umiko.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
