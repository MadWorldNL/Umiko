apiVersion: v1
kind: Service
metadata:
  name: {{ include "umiko.fullname" . }}
  labels:
    {{- include "umiko.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "umiko.selectorLabels" . | nindent 4 }}
